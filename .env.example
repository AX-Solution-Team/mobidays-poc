# PostgreSQL 연결 — Vercel Postgres / Neon / Supabase 등
# 로컬에서 dev 서버 띄울 때는 Postgres가 떠 있어야 합니다.
DATABASE_URL="postgresql://user:pass@host:5432/dbname?sslmode=require"

# (선택) /api/admin/seed 엔드포인트 보호용 토큰. 비워두면 누구나 시드 리셋 가능.
SEED_SECRET=""
